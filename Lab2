# Lab2
Lab2 for JiPP
#### Zadanie 1
```cpp
#include <iostream>
using namespace std;
#define MAX 100

int main(){
  cout<<"Vadym Kochenko Lab 1 Zadanie 1"<<endl;
  int arr[MAX];
  int n,i,j;
  int temp;
  
  cout<<"Prosze podac illosc elementow do sortowania: ";
  cin>>n;
  if(n<0 || n>MAX)
  {
    cout<<"Wpisz porzadnie"<<endl;
    return -1;
  }
  cout<<"Wpisz elementy: ";
  for(i=0;i<n;i++)
  {
    cin>>arr[i];
  }
  for(i=0;i<n;i++)
  {    
    for(j=i+1;j<n;j++)
    {
      if(arr[i]>arr[j])
      {
        temp=arr[i];
        arr[i]=arr[j];
        arr[j]=temp;
      }
    }
  }
  cout<<"Uporzadkowane liczby:"<<endl;
  for(i=0;i<n;i++)
    cout<<arr[i]<<" ";
  cout<<endl;  
  return 0;
  
}
```
